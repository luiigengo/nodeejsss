<%- include("partials/header") %>
<div class="container-fluid">
  <% posts.forEach(function (post) { %>

  <h1><%= post.title %></h1>
  <p><%= post.postText %></p>
</div>
<%})%> <%- include("partials/footer") %>
